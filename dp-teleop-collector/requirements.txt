# Additive dependencies for the existing leaptele environment. The repository's
# camera/LEAP packages must already be installed; keeping this file additive
# avoids replacing a working NumPy/OpenCV stack used by other LEAP tools.
pyrealsense2==2.58.3.10794
websockets>=12.0,<17.0

# Diffusion Policy uses the Zarr v2 API. Zarr 3 is intentionally excluded.
zarr==2.18.3
numcodecs==0.13.1
